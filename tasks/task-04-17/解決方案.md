
# 問題

1. @ D:\repo-project\ai-auto-video\app-backend\app\api\v1\routes\chat.py 當這支後端程式接收到 /clear 是否沒有使用 
### 載入工具指令定義

```python
_tool_commands: set[str] = set()

if settings.SYSTEM_TOOLS:

    _tools_data = json.loads(Path(settings.SYSTEM_TOOLS).read_text(encoding="utf-8"))

    _tool_commands = {t["command"] for t in _tools_data.get("tools", [])}
```


# 需求

1. 檢核程式碼是否有使用  tool_commands 機制呼叫 tools?
2. 如果沒有。思考如何解決方案。
3. 設計目的是前端透過 /[command] 尋找 .env 
    SYSTEM_TOOLS=D:\repo-project\ai-auto-video\app-backend\system-env\tools.json
    
```josn
# D:\repo-project\ai-auto-video\app-backend\system-env\tools.json
{

  "tools": [

    {

      "command": "/clear",

      "description": "清除對話歷史，保留系統提示詞",

      "action": "clear_messages"

    }

  ]

}
```

目的是實作類似 Function Call 的機制。還是有其他設計手法?

# 執行

1. 依據需求評估執行方案(審慎評估)
2. 評估目前的架構是否可以實作。
3. 可以列出實作步驟讓使用者確認是否執行。
4. 不可以列出問題點。